//! The module containing all calculation logic.
//!
//! This includes the parsing of strings, converting them to an internal
//! format, and performing the operation.

pub mod calculation;
pub mod executor;
pub mod parser;
